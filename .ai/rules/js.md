---
paths:
  - 'resources/js/**'
---

# Js

## TypeScript as frontend default
Create and modify application frontend code in TypeScript. Use <script setup lang="ts"> in new Vue components, and keep the Vite entry point at resources/js/app.ts. Existing Jetstream scaffold files may be migrated incrementally when changed.
